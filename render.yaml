services:
  - type: web
    name: constitutional-reliability-rag
    runtime: python
    plan: free

    buildCommand: |
      pip install --upgrade pip
      pip install .

    startCommand: uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT